# Bunny Stream library ID used by <BunnyVideo videoId="..." />, unless a
# libraryId is passed explicitly per video. Find this in the Bunny dashboard
# under Stream > your library > "Video Library ID".
PUBLIC_BUNNY_LIBRARY_ID=
